import { Constellations, Polygonal } from '../src';

const foo = new Polygonal({
	container: '#foo',
	width: '100%',
	height: '100%',
	seed: 6000,
	color: [0,0,255,0.8],
	debug: false,
	light: [16,-12],
	mouse: true
});
